import 'dart:io';

//import 'package:tflite/tflite.dart';
/*
void loadModel() async{
  var mymodel = await Tflite.loadModel(model: 'resources/labels_mobilenet_quant_v1_224.txt', labels: 'resources/mobilenet_v1_1.0_224_quant.tflite');
  print("result after loading MLmodel  ---  $mymodel");
}

void runModel(File cap_image) async {
  var res = await Tflite.runModelOnImage(path: cap_image.path);
  if(res!=null)
    {
      String name = res[0]["label"];
      double confidence = res[0]["confidence"];
      print("Name = $name");
      print("Confidence = $confidence");
    }

}
 */
